---
ngxs_config_dir: /etc/nginx/conf.d
ngxs_cache_dir:
  RedHat: /var/cache/nginx
  Debian: /var/cache/nginx
ngxs_cache_dir_owner:
  RedHat: nginx
  Debian: www-data
ngxs_cache_dir_group:
  RedHat: nginx
  Debian: www-data
